apiVersion: v1
kind: ConfigMap
metadata:
  name: radius-config
  
data:
  clients.conf: |
    client main {
      ipaddr = 0.0.0.0/0
      secret = testing123
    }
  users.conf: |
    testuser Cleartext-Password := "testpassword"
      Reply-Message = "Hello, %{User-Name}"